dist: xenial
addons:
  apt:
    packages:
      - libtool-bin

install:
  - flex --version
  - bison --version
  - autoconf --version
  - automake --version
  - libtool --version
  - m4 --version
  - pkgconfig --version
